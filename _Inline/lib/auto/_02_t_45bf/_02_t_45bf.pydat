namespace : %
    classes : %
    functions : @
        docopt
        get_keys
        get_val
        get_hash
filtered : <<EOV
from docopt import docopt

def get_keys(o):
    return list(o.keys())

def get_val(o, k):
    return o[k]

def get_hash(o):
    return list(o.items())

EOV
