namespace : %
    classes : %
    functions : @
        pydocopt
        wrap_docopt
filtered : <<EOV
from docopt import docopt as pydocopt

def wrap_docopt(doc, argv=None, help=True, version=None, options_first=False):
    return dict(pydocopt(doc, argv, help, version, options_first))
EOV
