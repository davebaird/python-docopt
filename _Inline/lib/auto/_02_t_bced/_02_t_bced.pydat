namespace : %
    classes : %
    functions : @
        docopt
        get_keys
        get_val
filtered : <<EOV
from docopt import docopt

def get_keys(o):
    print(o.keys())

def get_val(o, k):
    print(o[k])
EOV
